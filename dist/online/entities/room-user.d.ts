export declare class RoomUser {
    token: string;
    user_id: string;
    room_id: string;
    socket_id: string;
    constructor(token: string, user_id: string, room_id: string, socket_id: string);
}
